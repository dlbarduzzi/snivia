import { env } from "@/lib/client/env"

export const siteConfig = {
  url: env.NEXT_PUBLIC_APP_URL,
  name: "Snivia",
  description: "A digital storage platform designed to help users organize and access everything that matters.",
}
